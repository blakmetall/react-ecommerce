import styled from 'styled-components';

const CartWrapper = styled.div`
    padding: 20px;
    border: 1px solid #dcdcdc;
    margin-bottom: 150px;
`;

const StyledTotalWrapper = styled.div`
    padding-left: 120px;
`;

const StyledTotal = styled.div`
    font-weight: bold;
    font-size: 25px;
`;

export { CartWrapper, StyledTotalWrapper, StyledTotal };
